// Simulated MikroORM entity shipped as a `.d.ts` declaration file inside an
// external npm package (mirrors `@dudousxd/nestjs-durable-store-mikro-orm`).
// Declaration files retain the property declarations (the decorators are kept
// in the emitted `.d.ts` for MikroORM entities).
export declare class WorkflowRunEntity {
  id: number;
  name: string;
  status: string;
  attempts: number;
  createdAt: Date;
}
